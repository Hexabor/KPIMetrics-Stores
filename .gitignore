# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
*.swp

# Data files (no subir CSVs pesados)
data/*.csv
data/*.json
!data/categories-supercategories.json
!data/captacion-store-aliases.json

# Backups locales
*.backup.json

# Node modules (Vitest, solo herramienta de dev — no llega al cliente)
node_modules/

# Scripts puntuales con credenciales (uso unico, borrar tras usar)
schema-extract.php
db-setup.php

# Backend: credenciales y secretos (NUNCA a git, el repo es publico)
config.php

